#include<iostream>
using namespace std;
int main()
{
	int nomor;
	cin>>nomor;
	
    if (nomor == 6)
    {
        cout<<"ini adalah angka 6 ";
    }
    else if()
    {
        cout<<"ini adalah bilangan genap";  //else if artinya "kalau tidak" berarti kalau if di atas di run maka else if tidak akan di run
    }
    else{
        cout<<"ini bukan angka 6"; // else tidak perlu kondisi , karena ini adalah statement yang akan di run ketika  if atau else if tidak di run.
    }
    return 0;
}
